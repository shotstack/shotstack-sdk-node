/**
 * Shotstack
 * Official Node SDK for the Shotstack Cloud Video Editing API
 *
 * The version of the OpenAPI document: v1
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import HeyGenGeneratedAssetOptions from './HeyGenGeneratedAssetOptions';

/**
 * The HeyGenGeneratedAsset model module.
 * @module model/HeyGenGeneratedAsset
 * @version 0.2.6
 */
class HeyGenGeneratedAsset {
    /**
     * Constructs a new <code>HeyGenGeneratedAsset</code>.
     * Generate assets using HeyGen. HeyGen provide a text-to-avatar service.  The HeyGen provider works on a bring-your-own-key basis, credentials are required and must be added via the  [dashboard](https://dashboard.shotstack.io/integrations/heygen), not in the request.
     * @alias module:model/HeyGenGeneratedAsset
     * @param provider {module:model/HeyGenGeneratedAsset.ProviderEnum} The name of the provider - set to `heygen` for HeyGen.
     * @param options {module:model/HeyGenGeneratedAssetOptions} 
     */
    constructor(provider, options) { 
        
        HeyGenGeneratedAsset.initialize(this, provider, options);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, provider, options) { 
        obj['provider'] = provider || 'heygen';
        obj['options'] = options;
    }

    /**
     * Constructs a <code>HeyGenGeneratedAsset</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/HeyGenGeneratedAsset} obj Optional instance to populate.
     * @return {module:model/HeyGenGeneratedAsset} The populated <code>HeyGenGeneratedAsset</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new HeyGenGeneratedAsset();

            if (data.hasOwnProperty('provider')) {
                obj['provider'] = ApiClient.convertToType(data['provider'], 'String');
            }
            if (data.hasOwnProperty('options')) {
                obj['options'] = HeyGenGeneratedAssetOptions.constructFromObject(data['options']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>HeyGenGeneratedAsset</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>HeyGenGeneratedAsset</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of HeyGenGeneratedAsset.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['provider'] && !(typeof data['provider'] === 'string' || data['provider'] instanceof String)) {
            throw new Error("Expected the field `provider` to be a primitive type in the JSON string but got " + data['provider']);
        }
        // validate the optional field `options`
        if (data['options']) { // data not null
          HeyGenGeneratedAssetOptions.validateJSON(data['options']);
        }

        return true;
    }

/**
     * Returns The name of the provider - set to `heygen` for HeyGen.
     * @return {module:model/HeyGenGeneratedAsset.ProviderEnum}
     */
    getProvider() {
        return this.provider;
    }

    /**
     * Sets The name of the provider - set to `heygen` for HeyGen.
     * @param {module:model/HeyGenGeneratedAsset.ProviderEnum} provider The name of the provider - set to `heygen` for HeyGen.
     */
    setProvider(provider) {
        this['provider'] = provider;
        return this;
    }
/**
     * @return {module:model/HeyGenGeneratedAssetOptions}
     */
    getOptions() {
        return this.options;
    }

    /**
     * @param {module:model/HeyGenGeneratedAssetOptions} options
     */
    setOptions(options) {
        this['options'] = options;
        return this;
    }

}

HeyGenGeneratedAsset.RequiredProperties = ["provider", "options"];

/**
 * The name of the provider - set to `heygen` for HeyGen.
 * @member {module:model/HeyGenGeneratedAsset.ProviderEnum} provider
 * @default 'heygen'
 */
HeyGenGeneratedAsset.prototype['provider'] = 'heygen';

/**
 * @member {module:model/HeyGenGeneratedAssetOptions} options
 */
HeyGenGeneratedAsset.prototype['options'] = undefined;





/**
 * Allowed values for the <code>provider</code> property.
 * @enum {String}
 * @readonly
 */
HeyGenGeneratedAsset['ProviderEnum'] = {

    /**
     * value: "heygen"
     * @const
     */
    "heygen": "heygen"
};



export default HeyGenGeneratedAsset;

